import type { MonthlyEvolutionSeries } from '../types';

export const MONTHLY_EVOLUTION_SERIES: Record<MonthlyEvolutionSeries, boolean> = {
  atualDia: true,
  atual: true,
  anterior: true,
  previsto: true,
};
